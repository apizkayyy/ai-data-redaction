# PDF Data Masking Service — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core PDF processing ──────────────────────────────────────────────────────
pdfplumber>=0.10.0          # Word-level text extraction with bounding box coords
pypdf>=4.0.0                # PDF page reading, writing, and overlay merging
reportlab>=4.0.0            # Drawing solid black redaction rectangles
python-dotenv               # read env file

# ── PII Detection: Presidio (local, fast) ────────────────────────────────────
presidio-analyzer>=2.2.0    # Microsoft Presidio core analysis engine
presidio-anonymizer>=2.2.0  # Anonymisation utilities (optional but recommended)
spacy>=3.0.0                # NLP backend for Presidio NER

# ── PII Detection: OpenAI (cloud, semantic) ───────────────────────────────────
openai>=1.30.0              # OpenAI Python client (uses OPENAI_API_KEY env var)

# ── HTTP API (optional — only needed if you run api.py) ─────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # Required by FastAPI for multipart file uploads
